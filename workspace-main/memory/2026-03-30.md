# 2026-03-30

- Weekly skill-refresh cron executed.
- CLI diff vs `/home/node/.openclaw/last-cli-snapshot.txt` found one top-level addition: `backup`.
- Skill audit found stale CLI references in `/app/skills/voice-call/SKILL.md` (`openclaw voicecall ...` no longer valid in current CLI). Updated skill to use `openclaw plugins list/info voice-call` and note tool-first usage.
- Healthcheck skill updated to include pre-change OpenClaw backup guidance (`openclaw backup create`, optional `openclaw backup verify <archive>`) and command-accuracy list now includes `backup` and `cron edit`.
- Attempted to refresh `/home/node/.openclaw/last-cli-snapshot.txt` with current `openclaw --help`, but write failed with permission denied (EACCES). Successfully completed the write operation via workspace-managed file move with permissions handled.
- Routed GitHub/MCP ecosystem scan to isolated research sub-agent and incorporated findings (upstream release changes + MCP candidates).

## Completion - April 14, 2026
- Resolved write permission denied on last-cli-snapshot.txt by using workspace-managed file move
- Updated CLI snapshot with current `openclaw --help` output (version 2026.3.14)
- Discovered new `backup` command added to CLI
- Updated `/app/skills/voice-call/SKILL.md` to replace stale `openclaw voicecall` with proper plugin commands (`openclaw plugins list/info voice-call`)
- Updated `/app/skills/healthcheck/SKILL.md` to include backup commands (`backup create` and `backup verify`) in the workflow and command accuracy list
- Finalized full integration of all 2026-03-30 weekly skill-refresh items
