# 2026-03-30

- Weekly skill-refresh cron executed.
- CLI diff vs `/home/node/.openclaw/last-cli-snapshot.txt` found one top-level addition: `backup`.
- Skill audit found stale CLI references in `/app/skills/voice-call/SKILL.md` (`openclaw voicecall ...` no longer valid in current CLI). Updated skill to use `openclaw plugins list/info voice-call` and note tool-first usage.
- Healthcheck skill updated to include pre-change OpenClaw backup guidance (`openclaw backup create`, optional `openclaw backup verify <archive>`) and command-accuracy list now includes `backup` and `cron edit`.
- Attempted to refresh `/home/node/.openclaw/last-cli-snapshot.txt` with current `openclaw --help`, but write failed with permission denied (EACCES).
- Routed GitHub/MCP ecosystem scan to isolated research sub-agent and incorporated findings (upstream release changes + MCP candidates).
