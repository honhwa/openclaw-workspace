# 2026-03-19

- Ran weekly skill-refresh audit.
- OpenClaw CLI diff vs last snapshot: new top-level commands `backup` and `ltm`; no removals.
- Audited all 53 skills for invalid top-level OpenClaw command references.
- Updated stale skills:
  - `/app/skills/voice-call/SKILL.md`: removed legacy `openclaw voicecall ...` usage; switched to plugin management + `voice_call` tool guidance.
  - `/app/skills/healthcheck/SKILL.md`: added backup readiness/verification flow using new `openclaw backup` commands.
- Ran MCP ecosystem check (subagent-assisted + GitHub API queries).
  - High-signal sources: `modelcontextprotocol/servers`, MCP Registry, `punkpeye/awesome-mcp-servers`, `openclaw/acpx`.
  - No official `openclaw/*mcp*` server repo found in org search.
- Could not update `/home/node/.openclaw/last-cli-snapshot.txt` due filesystem permission (`EACCES`); wrote refreshed snapshot draft to `/home/node/.openclaw/workspace-main/last-cli-snapshot.new.txt`.
